#include <vector>

using std::vector;


void randomized_depth_first_search(size_t width, size_t height, size_t startX=0, size_t startY=0);

void kruskal(size_t width, size_t height);

void prim(size_t width, size_t height, size_t startX=0, size_t startY=0);